config:
  limits.cpu: "2"
  limits.memory: "512MB"    

  user.network-config: |
    version: 1
    config:      
      # outbound Internet
      - type: physical
        name: eth0
        subnets:
          - type: dhcp
            ipv4: true
            control: auto   

description:
devices:
  # bitcoin host needs outbound access
  eth0:
    nictype: bridged
    parent: managernet
    type: nic
  root:
    path: /
    pool: bcm_data
    type: disk
name: lxd_profile_proxy_host
